# Copiar para .env.local (nunca commitar o .env.local real).

# Chaves públicas — seguras para expor no browser (protegidas por RLS).
# Encontram-se em Supabase → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://echrxirbkpworpgwehkb.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Só para uso em scripts/servidor — nunca expor ao browser, nunca prefixar com NEXT_PUBLIC_.
# Contorna RLS: usar apenas no script de ingestão (scripts/checklist_mensal.py) e
# em rotas de servidor que precisem de escrever em obrigacoes/fundos/alertas.
SUPABASE_SERVICE_ROLE_KEY=
